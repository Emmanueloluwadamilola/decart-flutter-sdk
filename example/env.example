# Production (recommended): public endpoint that returns short-lived ek_ tokens.
# DECART_TOKEN_ENDPOINT=https://your-backend.com/decart/client-token

# For a local debug prototype instead, leave this file unchanged and paste a
# temporary dct_ test key into _developmentApiKey in lib/main.dart. Never commit
# that real key or distribute the resulting debug build.
